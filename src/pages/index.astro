---
import Layout from "../layouts/Layout.astro";
import { companyInitiators, donationUrl, personInitiators, supporters } from "../data/home";
import Donations from "../components/Donations.astro";
import toggle from "/images/toggle.svg";
---

<Layout>
  <main>
    <section class="tablet:hidden section section--secondary py-[40px]">
      <div class="section__content text-center">
        <a class="button button--primary" href={donationUrl}>Darovať teraz</a>
      </div>
    </section>

    <section class="section section--hero">
      <div class="section__content">
        <h1>Veríš v Slovensko?</h1>

        <p class="mt-[24px]">
          Hnev na nefungujúci štát, obava z kolapsu zdravotníctva či nenávisť voči inakosti… to všetko okrem iného
          spôsobuje, že v Slovensko v skutočnosti verí veľmi málo ľudí.
        </p>

        <h2 class="mt-[24px]">Poďme si navzájom vrátiť vieru v Slovensko!</h2>

        <ul class="mt-[8px]">
          <li>Pretože ak veríme, <strong>tak sa zaujímame</strong>.</li>
          <li>Pretože ak veríme, <strong>tak niečo dobré urobíme</strong>.</li>
          <li>Pretože ak veríme, <strong>tak neodídeme</strong>.</li>
          <li>Ak veríme, <strong>tak sa zapneme</strong>.</li>
        </ul>

        <a
          class="button button--secondary mt-[48px] inline-flex items-center justify-center gap-[10px]"
          href="/zapni-sa">Zapnime sa <img class="inline" src={toggle} /></a
        >
      </div>
    </section>

    <section id="zapnime_sa" class="section">
      <div class="section__content">
        <h2>Zapnime sa</h2>

        <p class="max-w-[800px] mx-auto mt-[40px] text-center">
          Poďme spolu robiť užitočné veci tak, aby sme dlhodobo posilňovali dôveru medzi nami. Tak, aby sa zapojil
          každý, kto svoju vieru v Slovensko bude chcieť pretaviť v činy. Dajme si výzvu a urobme spolu 1000 dobrých
          skutkov!
        </p>

        <div class="mt-[80px] grid grid-cols-1 tablet:grid-cols-3 justify-items-center gap-[32px]">
          <div
            class="w-full flex flex-col justify-between items-center pt-[42px] pb-[34px] px-[16px] text-center bg-secondary rounded-[8px]"
          >
            <h3 class="mb-[50px] text-[32px] font-medium text-primary">Podporím</h3>
            <a class="button button--primary" href={donationUrl}>Darujem</a>
          </div>

          <div
            class="w-full flex flex-col justify-between items-center pt-[42px] pb-[34px] px-[16px] text-center bg-secondary rounded-[8px]"
          >
            <h3 class="mb-[50px] text-[32px] font-medium text-primary">Urobím</h3>
            <a class="button button--primary" href="/zapni-sa">Vyberiem si</a>
          </div>

          <div
            class="w-full flex flex-col justify-between items-center pt-[42px] pb-[34px] px-[16px] text-center bg-secondary rounded-[8px]"
            x-data=""
          >
            <h3 class="mb-[50px] text-[32px] font-medium text-primary">Prevezmem zodpovednosť</h3>

            <button class="button button--primary" @click="$refs.voteModal.showModal()">Pôjdem voliť</button>

            <dialog x-ref="voteModal" class="vote-modal">
              <div class="flex justify-end mb-[20px]">
                <button @click="$refs.voteModal.close()">
                  <svg width="31" height="31" viewBox="0 0 31 31" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <line x1="1.14217" y1="1.14214" x2="29.4264" y2="29.4264" stroke="black" stroke-width="2"></line>
                    <line x1="29.4263" y1="1.14213" x2="1.14207" y2="29.4264" stroke="black" stroke-width="2"></line>
                  </svg>
                </button>
              </div>
              <script type="module" hoist>
                const observer = new IntersectionObserver(entries => {
                  for (const entry of entries) {
                    if (!entry.isIntersecting) continue;
                    observer.disconnect();
                    import("add-to-calendar-button");
                  }
                });
                const instances = document.querySelectorAll("add-to-calendar-button");
                for (const instance of instances) observer.observe(instance);
              </script>

              <add-to-calendar-button
                name="Parlamentné voľby 2023"
                startDate="2023-09-30"
                endDate="2023-09-30"
                timeZone="Europe/Bratislava"
                options="'Apple','Google','iCal','Outlook.com','Yahoo','Microsoft365','MicrosoftTeams'"
                hideBackground
                buttonsList
                size="7"
                label="Pridať do kalendára"></add-to-calendar-button>
            </dialog>
          </div>
        </div>
      </div>
    </section>

    <section id="pozri_kto_veri" class="section section--primary">
      <div class="section__content">
        <h2>Pozrite sa, kto všetko už v Slovensko verí</h2>

        <div
          class="mt-[100px] grid grid-cols-1 tablet:grid-cols-2 laptop:grid-cols-3 desktop:grid-cols-4 gap-[32px]"
          x-data="{ beliefs: $persist([]) }"
          x-init="fetch('/api/beliefs').then(response => response.json()).then(data => beliefs = data)"
        >
          <template x-for="belief in beliefs" :key="belief.id">
            <div class="bg-white rounded-[8px] overflow-clip flex flex-col">
              <img class="object-cover" style="aspect-ratio: 1;" x-bind:src="belief.image" />

              <div class="p-[24px]">
                <div
                  class="font-accent text-primary text-[32px] font-medium leading-snug uppercase tracking-[-1px]"
                  x-text="belief.name"
                >
                </div>
                <div class="mt-[12px] text-[18px] leading-snug" x-text="belief.organization"></div>
                <p class="mt-[24px] text-[18px] font-bold leading-normal" x-text="belief.text"></p>
              </div>
            </div>
          </template>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="section__content">
        <h2 class="mb-[100px]">V čo na Slovensku veríte vy?</h2>
        <Donations />
      </div>
    </section>

    <section id="kto_sme" class="section section--primary">
      <div class="section__content">
        <h2>Iniciátorky a iniciátori</h2>

        <p class="mt-[80px] tablet:text-center font-light">
          Sme jednotlivci, občianska spoločnosť, firmy, organizácie, inštitúcie a iniciatívy, ktorí veríme v Slovensko.
        </p>

        <p class="mt-[40px] tablet:text-center font-medium">
          {personInitiators.join(" • ")}
        </p>

        <p class="mt-[40px] tablet:text-center font-medium">
          {companyInitiators.join(" • ")}
        </p>
      </div>
    </section>

    <section id="podporovatelia" class="section">
      <div class="section__content">
        <h2>Podporovatelia</h2>

        <div class="mt-[100px] grid grid-cols-1 tablet:grid-cols-3 desktop:grid-cols-4 gap-[20px]">
          {
            supporters.map(supporter => (
              <div class="px-[16px] py-[24px] flex flex-col gap-[20px] items-center bg-violet-50 rounded-[8px]">
                <div class="text-[18px] font-bold leading-relaxed">{supporter.name}</div>
                <div class="text-[22px] font-medium leading-relaxed">{supporter.amount}</div>
                <p class="text-[14px] font-medium leading-normal">{supporter.note}</p>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <section id="kontakt" class="section section--primary pt-[80px]">
      <div class="section__content">
        <h2>Kontakt</h2>

        <p class="mt-[40px] font-light text-center">Ďakujeme, že veríte v Slovensko.</p>

        <p class="mt-[20px] font-light text-center">
          Email: <a class="font-semibold hover:underline underline-2 underline-offset-4" href="mailto:ahoj@zapnisa.sk">ahoj@zapnisa.sk</a>
        </p>

        <p class="mt-[20px] font-light text-center">
          Pre média: <a class="font-semibold hover:underline underline-2 underline-offset-4" href="mailto:media@zapnisa.sk">media@zapnisa.sk</a>
        </p>
      </div>
    </section>

    <section class="section section--secondary py-[80px]">
      <div class="section__content text-center">
        <a class="button button--primary" href={donationUrl}>Darujte teraz</a>
      </div>
    </section>
  </main>
</Layout>
